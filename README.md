# DGMon

Flutter expense tracker starter.

## Current Features

- Track `In` and `Out` transactions
- Create custom categories per transaction type
- Create multiple cash accounts and track each balance
- Overview page with total balance, totals by type, and recent transactions
- Transaction list with swipe-to-delete

## Run

```bash
flutter run
```
